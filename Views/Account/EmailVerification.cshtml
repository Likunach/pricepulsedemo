@{
    ViewData["Title"] = "PricePulse - Email Verification";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title>@ViewData["Title"]</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&amp;display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script>
        tailwind.config = {
          theme: {
            extend: {
              colors: {
                "primary": "#5d288a",
                "accent": "#f94037",
                "background-light": "#eff9f2",
                "secondary-accent": "#0cb8ca",
              },
              fontFamily: {
                "display": ["Inter", "sans-serif"]
              },
              borderRadius: {
                "DEFAULT": "0.5rem",
                "lg": "0.75rem",
                "xl": "1rem",
                "full": "9999px"
              },
            },
          },
        }
    </script>
    <style>
        .material-symbols-outlined {
          font-variation-settings:
            'FILL' 0,
            'wght' 400,
            'GRAD' 0,
            'opsz' 24
        }
    </style>
</head>
<body class="bg-background-light font-display text-slate-800">
    <div class="flex flex-col min-h-screen">
        <header class="bg-background-light/80 backdrop-blur-sm sticky top-0 z-10">
            <div class="container mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between h-16">
                    <div class="flex items-center gap-3">
                        <div class="flex items-center justify-center size-10 bg-primary text-white rounded-full">
                            <span class="material-symbols-outlined text-xl">trending_up</span>
                        </div>
                        <h1 class="text-2xl font-bold text-primary">PricePulse</h1>
                    </div>
                    <div class="flex items-center gap-2">
                        <button class="inline-flex md:hidden items-center justify-center size-10 rounded-full hover:bg-primary/10 transition-colors">
                            <span class="material-symbols-outlined text-2xl text-primary">menu</span>
                        </button>
                    </div>
                </div>
            </div>
        </header>
        
        <main class="flex-grow flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
            <div class="w-full max-w-lg mx-auto p-8 md:p-12 bg-white rounded-xl shadow-2xl text-center">
                @if (TempData["Message"] != null)
                {
                    <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
                        @TempData["Message"]
                    </div>
                }

                @if (TempData["Error"] != null)
                {
                    <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
                        @TempData["Error"]
                    </div>
                }

                <div class="mb-8">
                    <div class="mx-auto flex items-center justify-center size-24 bg-secondary-accent/10 rounded-full">
                        <span class="material-symbols-outlined text-6xl text-secondary-accent">outgoing_mail</span>
                    </div>
                </div>
                
                <h2 class="text-4xl font-black text-primary mb-4">Check Your Inbox!</h2>
                <p class="text-slate-600 mb-8 text-lg">
                    We've sent a verification link to <strong class="text-accent">@(ViewBag.Email ?? "your.email@example.com")</strong>. Click the link to unlock the full power of PricePulse.
                </p>
                
                <div class="flex flex-col items-center gap-4">
                    <form method="post" action="@Url.Action("ResendVerification", "Account")" class="w-full">
                        <input type="hidden" name="email" value="@ViewBag.Email" />
                        <button type="submit" class="w-full flex items-center justify-center gap-3 rounded-lg h-14 px-8 bg-accent text-white text-lg font-bold shadow-lg hover:bg-accent/90 focus:outline-none focus:ring-4 focus:ring-accent/50 transition-all transform hover:scale-105">
                            <span>Resend Verification Email</span>
                        </button>
                    </form>
                    <p class="text-sm text-slate-500">Didn't receive it? Check your spam folder or wait a few minutes.</p>
                </div>
            </div>
        </main>
        
        <footer class="bg-transparent mt-auto">
            <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-6 text-center text-sm text-slate-500">
                <p>© 2025 PricePulse. All rights reserved.</p>
            </div>
        </footer>
    </div>
</body>
</html>